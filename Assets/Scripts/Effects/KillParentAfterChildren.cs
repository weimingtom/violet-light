using UnityEngine;
using System.Collections;

public class KillParentAfterChildren : MonoBehaviour {
	
	// Update is called once per frame
	void Update () 
    {
        /*foreach( Transform child in transform )
        {
            return;
        }
        Destroy( gameObject );
        */
	}
}
