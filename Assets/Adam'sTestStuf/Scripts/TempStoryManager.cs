using UnityEngine;
using System.Collections;

public class TempStoryManager : MonoBehaviour {
	PortraitManager manager;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
		manager.Enter (0, 0);
	}
}
